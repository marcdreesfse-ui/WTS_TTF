fontversion.json is the file which tells WTS what the lates font version is.
wanderstuck.ttf is the font WTS uses to display the correct characters. this file is also the file which gets downloaded by WTS
